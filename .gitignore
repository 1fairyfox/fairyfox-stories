# ── Cross-project reference clones (shared standard) ─────────────────────────
# Read-only clones of OTHER repos (e.g. the fairyfox.io hub), pulled on
# demand. Never commit them — see the cross-project-sync standard.
assets/references/*
!assets/references/README.md

# ── OS / editor cruft ────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*~
.vscode/
.idea/

# ── Local secrets (writing/art API keys) ─────────────────────────────────────
.env


# ── Jekyll build output ──────────────────────────────────────────────────────
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
vendor/


# local jekyll serve log (preview workflow)
_site_serve.log
serve.log

