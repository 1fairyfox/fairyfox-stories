# 2026-07-06

## Greenfield bootstrap — the story farm

Set Fairy Fox Stories up from scratch, greenfield, after clearing a prior unauthorized scaffold.
Worked through requirements with Fairy Fox rather than running ahead (a correction from an earlier
overreach): confirmed it's an **AI story farm** — plant books (≤ every 3 days) and grow them a
chapter a day, over a meshed world of universes/characters/cities.

### Decisions locked (with Fairy Fox)

- **Greenfield + collection/monorepo**, key `fairyfox-stories`, public, MIT.
- **Form-first** books: roll short story / novelette / novella / **rare novel**; the form sets the
  structure. Full-spectrum word counts.
- **Jekyll** site (not static HTML) for data-driven meshing; **comprehensive** collections
  (books, chapters, universes, characters, cities).
- Blended **least-tended-first** picking (+15–20% random); weekly **25% sequel** roll; rare
  **no-sequel** marking. First-person real-character mandate. OpenAI for prose/art via `.env`.

### Built

- Craft body of knowledge (`reference/craft/`), operating model, universe system, reading
  experience; project context + decisions + status.
- Jekyll scaffold: `_config`, Gemfile, Pages workflow; collections; layouts (default/shelf/book/
  chapter/universe/character/city/blueprint) + mesh chrome includes; `home.css` + `reader.css`
  (drop cap, progress); `check-stories.test.mjs` integrity check.
- **Book #1 — "What the Tide Keeps"** (novelette, the Tidewrack Coast): universe + city + two
  characters, full public blueprint, book-notes, hand-drafted chapter 1 (1 of 7 → 14%).

### Verified

`npm test` green (5 checks); `bundle exec jekyll build` clean; all pages generate + cross-link.

### Next

Push `dev`; **ask Fairy Fox before releasing to `main`** (deploys Pages). Then: Chrome visual
pass, enable Pages, hub registration, add `.env` key, and begin the daily loop. Art is placeholder
(cover/header) until the OpenAI key is in.

## Later same day — Chrome QA, content bar, and a book swap

- **Chrome visual pass** (via the served site): shelf + reader confirmed. Fixed a real bug — a lone
  book card ballooned full-width because the theme's `.grid.cols-3` overrode the shelf grid; shelf
  now uses its own fixed-width grid. Drop cap, progress bar, and mesh links all render. Pushed to `dev`.
- **Content bar set** (owner): dark is fine, but **broadly accessible — no NSFW/gore/gruesome/
  obscene** ("dark but not adult-only", *Carmen Sandiego*). Encoded in `CLAUDE.md` + `craft/genre-range.md`.
- **Book #1 swapped** (owner feedback: grief/drowning is a downer, not the wanted tone). Retired
  *What the Tide Keeps* and its Tidewrack Coast universe entirely. Planted a new book #1 in the
  entertaining-dark spirit: **_The Cinderwick Job_** — a caper (novelette) in the new **Gaslight
  Reach** universe: Juno Vale, a sharp finder-thief, hired into a trap to steal a curio from the
  Ordinal's own auction house. New universe + city (Cinderwick) + two characters (Juno Vale,
  Magister Crane) + full blueprint + hand-drafted chapter 1 ("The Whistle"). Refined `genre-range.md`:
  **gripping before sad.**
