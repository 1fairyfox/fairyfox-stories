# 2026-07-10

## Site emblem put to use (masthead + social cards)

Fairy Fox added `assets/icon.png` (the Stories emblem — an open book sprouting a plant over a
shelved library) and asked to use it wherever needed. It was referenced nowhere. Put it in the
three real gaps:

- **Masthead** — added the emblem to the landing hero's existing `.mast-logo` slot in
  `_layouts/shelf.html`; a new `.mast-logo.emblem` rule in `home.css` un-rounds it and switches to
  `object-fit:contain` so the tall, transparent illustration shows whole (the theme default is a
  round 56px `cover` avatar, which clipped it badly).
- **Social share card** — set a site-wide default `image: /assets/icon.png` in `_config.yml`'s
  `defaults`, so `{% seo %}` finally emits `og:image` / `twitter:image` (nothing set `image`
  before; books use a custom `cover:` key seo-tag ignores, so they fall back to the emblem too).
- **Structured data** — set site `logo: /assets/icon.png` for the JSON-LD publisher logo.

Left the **shared hub fox** (`header.html` / `footer.html` → `fairyfox.io/assets/icons/fox.png`)
and the root favicons alone — cross-project branding, per Fairy Fox's reminder.

### Verified

`npm test` green (5 checks); `bundle exec jekyll build` clean; confirmed the built home page and a
book page carry `og:image` / `twitter:image` + a JSON-LD publisher logo pointing at the emblem;
previewed the masthead rendered against the real `styles.css` / `home.css` at desktop + mobile
(emblem uncropped, well-scaled, clearly distinct from the header fox).

### Next

Pushed to `dev`. **Ask Fairy Fox before releasing to `main`** (deploys Pages). Optional follow-up:
give books their own `cover:` art as a per-book `image:` so their share cards use the cover instead
of the generic emblem.
