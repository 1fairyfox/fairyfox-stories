# Changelog — 2026-08

Newest first. Plain-English, per substantive commit. No version bump = maintenance.

## 1.1.2 — Full fairyfox compliance audit + supply-chain fixes

PATCH. Ran the full **fairyfox compliance check** (every standard's `## Verify`, per-item) against
v1.1.1. Most standards `done`; the audit caught a supply-chain cluster that the post-adoption
manifest had marked implemented a shade too generously — all fixed here and shipped.

- **Every workflow `uses:` SHA-pinned** with a version comment. `pages.yml` and `release.yml` were
  still tag-pinned; pinned all six workflows and **bumped to latest majors** (checkout v7,
  attest-build-provenance v4, configure-pages v6, upload-pages-artifact v5, deploy-pages v5,
  setup-ruby v1).
- **Provenance now attaches the `.intoto.jsonl` as a release asset** in `release.yml` (Scorecard's
  Signed-Releases scores attestation-alone as 0). Confirmed the attest action's `bundle-path`
  output survives the v2→v4 bump.
- **New `scripts/check-standards.mjs`** — machine-checks the grep-able invariants (header
  active-nav; `VERSION` == newest `main` tag) — wired into `npm test` + CI (repo-hygiene).
- **Repo hardening via `gh`**: auto-delete-on-merge on; squash/rebase off (merge-commit-only);
  **`dev` protected** against deletion + force-push (no PR requirement, so direct commits still work).
- **Closed the 5 open Dependabot PRs** (#2–#6) — superseded by the direct latest-major SHA-pin;
  every open PR now merged-or-closed-with-reason (ship contract).
- `cookies.html` "Last updated" corrected to July 25, 2026 — the v1.0.0 content-change date
  (games→Service + clear/reset disclosure) it should have carried but didn't.
- Report: `notes/fairyfox-reports/2026-08-02-compliance-audit.md`. `npm test` + `jekyll build` green.
- **Remaining, named (not fixable here):** the 4 SaaS quality badges (owner creates
  Codecov/CodeFactor/SonarCloud + secrets); the docs-site chrome browser pass (no Chrome connected);
  the live `fairyfox.io/fairyfox-stories/` 404 (DNS/account-rename, owner-side); hub-side registration.

## 1.1.1 — Grow: *The Wind She Would Not Keep* ch.2 "The Sea-Wind" (2/3)

PATCH. The daily **GROW** run, second of two content runs on 08-02 (the sibling PLANT task shipped
*The Overcount* v1.1.0 earlier — see below). One whole chapter written to the least-tended book.

**Pick (logged):** override roll `68` (threshold ≤18) → **no override**; blend leader by the confirmed
formula `0.5·(remaining/planned) + 0.5·(days_stale/max_days_stale)`. *The Wind She Would Not Keep*
led at **0.633** (incompleteness 2/3 = 0.667; **9 days stale** since its 07-24 plant; max staleness 15 =
Cinderwick) — narrowly over *Every Lock but Hers* (0.627). Weekly sequel roll **not due** (ran earlier
today during the Bus grow; next on/after 08-09, and nothing eligible — all three completed series now
carry a growing-or-pending sequel, *Cartographer*'s now satisfied by *The Overcount*).

**The chapter (the complicity beat).** Three days into her false safety, Zari has traded fear for a
tally-against-a-promise and turned away three walk-ups; on the fourth morning Rafiq passes westbound
(one line, no scene). Then **Naima** — eleven, a hundred days across the waste — lays out the exact fare
coin by coin and asks for **the sea-wind** for her dying fisherman grandfather: the third bottle on the
high shelf, the very one now promised to Sabr. Zari has **every fact**; the sale would cost the whole
stall; the refusal costs her **nothing** (no trap, no lie — the bottle is genuinely not hers to sell
under the fair contract). She runs "the easiest sum there is" and refuses with words true and hollow —
"That one is promised." The child does not argue, only re-reckons gravely, and **thanks her for
looking.** Zari lets them go without calling after; back under the cloth her held-breath safety takes a
**hairline**, and she closes unable to name the wrong — "the exact condition of a person about to do
something expensive." Adem's spare ledger voice held; grief kept off-page and bright-before-sad; the
grandfather asks nothing and gives one level fisherman's look. Direct hand-off to ch.3's breaking.
`npm test` green (6 checks + check-links, 57 files); Jekyll build clean and the built ch.2 page
verified (title, prose, footer nav correct at the true last chapter — prev→ch.1 + back-to-book, no
dangling next). Chrome not connected — prose-only on the eye-verified chapter template, rides the
standing eyeball debt. Full audit in `sessions/2026-08/2026-08-02.md`.

## 1.1.0 — Plant: *The Overcount* (novella · the Long Quiet · Cartographer sequel-in-realm)

New book (MINOR). The **PLANT** run, taking the Cartographer sequel window the morning grow queued
(v1.0.1 below). *The Overcount* — a **generation-ship SF thriller** in **the Long Quiet**, a new,
standalone tale in the realm of *The Cartographer of Decks* (`sequelTo` marked; reads fully on its own).
Told by a **new author-persona, Aster Coyle** (taut, procedural SF), and in **present tense** — a first
for a shelf whose narrators all speak in past, chosen for a ticking hunt with no *later* to look back
from.

**Rolls (logged):** form `86` → novella; word-pick `15` → wordCap **21,000** (11 chapters, finale ~1.5×);
universe `63` → existing (fixed to the Long Quiet by the queued sequel — a **join** deepening a one-book
realm); author `6` → **new persona** (a sequel's byline runs the normal roll, per the 07-24 decision — so
a *different* teller than the parent's Idris Okonkwo-Vance); sequel-lock `18` → not locked.

**The book.** Wren Astel, 24, a teller of the **Muster** — the office holding the sealed *Ark Meridian*
balanced to the body, one birth licensed against one death (a freed **quittance**) — strikes a beloved
name and finds the ship breathing for **one body more than any register can name.** The overcount is
**twins on one licensed birth**: eighteen years hidden, one child entered and one, **Ilex**, kept in the
cold unmapped decks. Wren hunts him; the **complicity beat** lands (she sees him, files it by protocol,
pays nothing, can't unsee it); and the turn is care over order at a public price — she licenses Ilex
openly against his mother's own just-freed quittance and takes the fall. Antagonist **Provost Halden**
is right about the stakes and wrong about the only mercy; **Doss** proves the ecology bore the overcount
for eighteen years unharmed; **Sela Renn** returns for one sequel-tie scene (no re-reveal of
*Cartographer*'s secret). Full public blueprint; ch.1 "The Quittance" (~2,000 words) written to Coyle's
voice, planting the quittance engine and the warm-tomato Chekhov object.

**Wiring.** New `_books/the-overcount.md`, `stories/the-overcount/blueprint.md`,
`_chapters/the-overcount/01.md`, `notes/books/the-overcount/book-notes.md`, `_authors/aster-coyle.md`,
five `_characters/*` (wren-astel, ilex, ondine-verrine, provost-halden, doss); `_universes/the-long-quiet.md`
gains the **Muster** + a books/seeds update. Cover + ch.1 header generated (gpt-image-1; header 1536×1024)
and viewed clean. Shelf auto-lists from `_books`; README has no per-book list to touch. `npm test` green
(6 checks + check-links, 56 files); `jekyll build` clean. Chrome not connected — rides the standing
eyeball debt (no template/CSS change). `VERSION` 1.0.1 → **1.1.0**; released via `release/1.1.0` → PR →
tag **v1.1.0**.

## 1.0.1 — Grow: *The One-O'Clock Bus* ch.3 "The Rules of the Hour" (3/10)

Grow (PATCH). The daily `fairyfox-stories-daily` run — first run since 07-25 (the scheduled job did
not fire 07-26→08-01). Blend leader taken **straight, no override** (override roll `74` vs. the ≤18
threshold — does not fire): *The One-O'Clock Bus* led at **0.767** (incompleteness 8/10 = 0.800 ·
staleness 11/15 = 0.733, updated 07-22), the shelf's highest incompleteness by a distance and stale
besides — exactly the ch.3 the 07-25 status forecast. Runners-up: *Wind She Would Not Keep* 0.633,
*Every Lock but Hers* 0.627, *Cinderwick* 0.571, *Hundredth Wind* 0.552.

**Weekly sequel roll — DUE and RUN** (last roll 07-21; due on/after 07-28; the missed week made it
overdue). Only one eligible completed series: **The Cartographer of Decks** (*Launderette* and *Girl*
each already have a growing sequel, out of the roll). Roll `9` (≤25) → **FIRES**. This **queues a
pending sequel plant** for *Cartographer* (the Long Quiet's first sequel) — the GROW task does not
plant; the sibling `fairyfox-stories-new` task takes the window (next plant eligible on/after 07-27,
so the queue is live now). Recorded in status.md → Next.

**Ch.3 "The Rules of the Hour"** (~2,400 words) — training nights. Abel teaches Priya the covenant
(*drive the route as printed* — a promise kept precisely *because* no one is watching); night one is
`nil` (the hour touches nobody). Night two she sees it clean: the hour takes the regular **Cyril**
off the card to the all-night cab office for company (fare: "I don't much want to go home yet") — and
Abel tells his **first lie to someone he respects**, deflecting it as "discretion." The book's stated
asymmetry made canon: the Vera "nothing there" lie cost nothing and *kept*; the Priya lie "lands on
her" and "tasted of tin." Then the **Corvus escalation** — for the first time in eleven years 17 does
not just slow but **stops and opens her doors on an empty street**, on nobody, reaching for the one
passenger she's never delivered (Abel), with a witness in the cab; he doesn't rise, and the blue book
gains its first new word about Corvus: *Opened.* Priya asks the Corvus question (as the ledger
reserved for ch.3) and gets the same "Nothing there" — the two lies rhymed. Closes on **Priya finding
the blue notebook** open in her hand: "This isn't the route as printed." Direct handoff to ch.4.

`updated: 2026-08-02` on the book; state stays `growing` (3/10). `npm test` green (6 checks +
check-links, 54 files). Prose + front-matter only — no template/CSS change; Chrome not connected this
run, so the chapter page rides the standing eyeball debt (shares the verified shared chapter template).
Released the PATCH dev→main via PR, tag **v1.0.1**.
